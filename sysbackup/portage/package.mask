# Mask the upstream dist-kernel virtual so it doesn't try to force kernel upgrades
virtual/dist-kernel::gentoo

# firefox does not support ffmpeg 8
# https://bugzilla.mozilla.org/show_bug.cgi?id=1962139
# we must mask ffmpeg 8
#
>=media-video/ffmpeg-8
